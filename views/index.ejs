<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD"
      crossorigin="anonymous"
    />
    <script
      defer
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN"
      crossorigin="anonymous"
    ></script>
    <title>Message Board</title>
  </head>

  <body class="d-flex flex-column vh-100 bg-dark-subtle">
    <main class="container-xl">
      <h1 class="my-3 px-5 pb-2 border-bottom border-secondary">
        Message Board
      </h1>
      <div class="row">
        <div class="new-message-form col d-flex flex-column gap-3 me-3 p-0">
          <div class="d-flex flex-column border border-secondary rounded p-3">
            <h3 class="align-self-center pb-2 border-bottom border-secondary">
              New Message Form
            </h3>
            <form method="post" action="/new">
              <div class="mb-3">
                <label for="author" class="form-label">Screen Name</label>
                <input
                  type="text"
                  class="form-control"
                  id="author"
                  name="author"
                  aria-describedby="authorHelp"
                />
                <div id="authorHelp" class="form-text">
                  This will appear at the top of your message.
                </div>
              </div>
              <div class="mb-3">
                <label for="content" class="form-label">Message</label>
                <textarea
                  type="password"
                  class="form-control"
                  id="content"
                  name="content"
                ></textarea>
              </div>
              <button type="submit" class="btn btn-secondary">Submit</button>
            </form>
          </div>
          <div class="d-flex flex-column border border-secondary rounded p-3">
            <h3 class="align-self-center pb-2 border-bottom border-secondary">
              Project Info
            </h3>
            <p>Some info about what's going on and how the project was made</p>
          </div>
        </div>
        <div
          class="message-board col-lg-8 d-flex flex-column gap-4 border border-secondary rounded p-3 overflow-y-auto"
        >
          <% messages.forEach((message, index)=>{ %> <% if(index % 2 === 0){ %>
          <div class="card text-bg-secondary">
            <h3 class="card-header"><%= message.author %></h3>
            <p class="card-body"><%= message.content %></p>
            <span class="card-footer"
              ><%= message.createdAt.toLocaleString() %></span
            >
          </div>
          <% } else { %>
          <div class="card bg-secondary bg-opacity-25">
            <h3 class="card-header"><%= message.author %></h3>
            <p class="card-body"><%= message.content %></p>
            <span class="card-footer"
              ><%= message.createdAt.toLocaleString() %></span
            >
          </div>
          <% } %> <% }) %>
        </div>
      </div>
    </main>

    <footer class="footer mt-auto py-3 bg-light">
      <div
        class="container d-flex justify-content-center gap-5 border-top pt-2"
      >
        <span class="text-muted"
          >Created by
          <a href="https://github.com/kensonjohnson">Kenson Johnson</a></span
        >
        <span class="text-muted"
          >Source Code on
          <a href="https://github.com/kensonjohnson">GitHub</a></span
        >
      </div>
    </footer>
  </body>
</html>
